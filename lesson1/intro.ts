const user = { name: "Andrea", age: 49 }

console.log(user.name);
// this gives an error (the prop doesn't exist!). Nevertheless the command tsc intro.ts will give the output file
console.log(user.email); 